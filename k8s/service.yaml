apiVersion: v1
kind: Service
metadata:
  name: pr-tracker
  namespace: pr-tracker
  labels:
    app: pr-tracker
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: pr-tracker
